# Cat OS

An operating system implementation for [Cat Machine](https://github.com/Serble/CatMachine) designed to run
on the CatVM.

In addition to the kernel there are also some core pieces of software that glue everything together, for
example the firmware allows booting from disks as opposed to ROM files.
