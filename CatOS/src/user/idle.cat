; The idle process: always runnable, never blocks or exits. It exists so the
; scheduler always has something to run when every real process is blocked.
; It simply yields the CPU forever.
#include "lib/sys.cat"

idle_start:
    sys_yield
    jmp idle_start
